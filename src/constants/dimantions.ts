// Constants for screen size
export const SCREEN_SIZE = {
  WIDTH: window.innerWidth,
  HEIGHT: window.innerHeight,
};

export const MIN_RESPONSIVE_HEIGHT = {
  TABLET: 620,
};

export const HEADER_HEIGHT = 56;

export const MENU = {
  WIDTH: 60,
  ITEM_SIZE: 40,
};
